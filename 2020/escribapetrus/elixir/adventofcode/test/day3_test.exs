defmodule DayThreeTest do
  use ExUnit.Case
  # import Adventofcode.DayThree

  # test "checks the position in a line" do
  #   assert iter_line(0) == 0
  #   assert iter_line(1) == 3
  #   assert iter_line(2) == 6
  #   assert iter_line(10) == 30
  # end

end
